# 🧠 Сервер управления клиентами

> ⚠️ **Ничего не трогать!**  
> Здесь работает **Nox** все настройки и логика уже настроены.  
> Если ты не знаешь, зачем это — значит всё работает как надо, просто **не редактируй**.

---

## 🚀 Описание

Это серверная часть проекта, предназначенная для управления клиентами, которые подключаются по сети.  
Каждый клиент сообщает своё имя, уровень (`Level`) и режим работы (`Mode` — *Параллельно* или *Последовательно*).

Сервер может:
- Принимать подключения от клиентов  
- Отображать список активных клиентов  
- Отправлять им команды для выполнения (`WORK`)  
- Работать в **параллельном** или **последовательном** режиме

---

## 🖥 Интерфейс

> ⚠️ **Ведётся работа над новым визуалом.**
> Текущие окна являются тестовыми.

Главное окно показывает:
- IP-адрес и порт сервера  
- Список подключённых клиентов с их именем, уровнем и режимом  
- Кнопки управления:
  - 🔌 **Отключить выбранного**
  - 🧹 **Отключить всех**
  - ▶️ **Запросить do_work()** — отправить команду клиентам  

---

## ⚙️ Как это работает

1. Сервер автоматически определяет свой IP и слушает порт **9090**.  
2. При подключении клиент отправляет строку вида:
