# 🤖 Клиент для подключения к серверу

> ⚠️ **Не изменять этот файл!**  
> Это **шаблон** клиентской части.  
> Его нельзя редактировать — только **копировать** и использовать как основу для новых клиентов.  
>  
> 🧱 **Добавлять код можно только в `do_work()`, `CLIENT_NAME`, `CLIENT_LEVEL` и `CLIENT_MODE`.**
>  
> 💬 В дальнейшем интерфейс будет переработан и обновлён.

---

## 🚀 Назначение

Этот клиент предназначен для подключения к серверу управления, обмена командами и выполнения задач, отправленных сервером.  
Он используется как **базовый шаблон**, от которого можно создавать разные версии клиентов с индивидуальными параметрами.

---

## 🧩 Как работает

1. Клиент подключается к серверу по IP и порту:
   ```python
   SERVER_IP = socket.gethostbyname(socket.gethostname())
   PORT = 9090

